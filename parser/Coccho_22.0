hamlet(to, be, or, _not, to, be)
    that = is + the + question;

    whether:
    tis:
    nobler:
    in = the@(mind, to, suffer);
    the = slings * _and * arrows(of, outrageous) * (-fortune{or, to, take, arms});

    against:
    a_sea_of_troubles:
    _and = by_opposing * _end * them;

    to_die:
    to_sleep:
    return no_more;
end;